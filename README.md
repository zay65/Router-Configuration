# Router-Configuration
Repo where I configure a router with host/domain names, a local user acct with admin privileges; generated RSA keys &amp; enabled SSH version 2 and restricted access to it with an inactivity timeout; assigned IPV4/IPV6 addresses to the e0/0 interface
